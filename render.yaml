services:
  - type: web
    name: drought-dashboard
    env: python
    buildCommand: "pip install --upgrade pip setuptools wheel && pip install -r requirements.txt"
    startCommand: "gunicorn drought_dashboard:server"
    plan: free
